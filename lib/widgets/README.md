# Widgets Directory

This directory is reserved for **reusable Flutter widgets** that are shared across multiple screens.

## Planned Widgets (Post-MVP)
- `SolarScoreGauge` – Animated circular score display
- `MetricCard` – Standardized card for displaying solar metrics
- `BrandButton` – Consistent branded button component

> These components are currently inlined in their respective screens and will be extracted here as the codebase scales.
